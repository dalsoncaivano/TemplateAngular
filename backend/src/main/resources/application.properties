spring.application.name=backend
spring.config.import=optional:file:.env[.properties]

spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:fourtalentsacademy}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=${DB_TIMEZONE:UTC}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:!K27Zf49}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=${DB_CONNECTION_LIMIT:10}
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.pool-name=centro-formacao-pool

spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=${DB_TIMEZONE:UTC}

spring.sql.init.mode=never
